# Figures

**Superseded by STEP 9.** This file was written in STEP 1 as a planning placeholder
("no images exist here yet"). As of STEP 9, all 11 planned figures have been generated
from the locked STEP 8 `evaluation/figure_data/*.csv` contracts. See
`FIGURE_INDEX.md` for the authoritative, current description of every figure,
`FIGURE_METADATA.csv` for machine-readable metadata, and `FIGURE_CONTRACT_INVENTORY.md`
/ `FIGURE_NUMERIC_AUDIT.md` / `FIGURE_CLAIM_AUDIT.md` for the full provenance and audit
trail. The original STEP 1 planning notes are kept below for historical reference only.

---

## STEP 1 planning notes (historical, kept for reference)

This step only defines the plan and inventories what already
exists elsewhere that can likely be reused or regenerated rather than built from scratch.

## Existing figures already covering most of these categories

`research/prototype/final_demo_pack/figures/` (16 figures, generated by
`figures/generate_figures.py` from `metadata/computed_metrics.json`) and
`research/prototype/final_comparison/figures/` (11 figures, generated by
`scripts/generate_figures.py` from `tables/*.csv`) already cover nearly every category
below. Both are CPU-only (matplotlib), reproducible from frozen `outputs/` data, and
**frozen sources for this workspace** — regenerate them into `figures/` here to confirm
zero drift rather than copying blindly, or reference them directly if no drift is found.

## Planned categories and their nearest existing source

| Category | Nearest existing figure(s) |
|---|---|
| Overall original vs. current | `final_comparison/figures/01_overall_metric_comparison.png` |
| Evidence coverage | `final_demo_pack/figures/01_evidence_coverage_v0_to_v1.png`, `02_evidence_coverage_across_natural_batches.png`; `final_comparison/figures/02_evidence_coverage_comparison.png` |
| Verifier performance | `final_demo_pack/figures/03_bare_vs_labeled_verification_outcomes.png`, `06_contradiction_detection_comparison.png`, `10_threshold_sensitivity_curve.png` |
| Verdict distributions | `final_comparison/figures/03_verdict_distribution_comparison.png` |
| Correction funnel | `final_demo_pack/figures/14_correction_pipeline_funnel.png`; `final_comparison/figures/04_correction_funnel_comparison.png` |
| Safety outcomes | `final_demo_pack/figures/09_safety_outcomes.png`; `final_comparison/figures/06_safety_comparison.png` |
| Confidence distributions | `final_comparison/figures/07_confidence_distribution.png` |
| Ablation results | `final_comparison/figures/08_ablation_comparison.png` |
| Runtime/resource comparison | `final_comparison/figures/09_runtime_resource_comparison.png` |
| Natural-data results | `final_comparison/figures/10_cumulative_natural_data_results.png` |
| Synthetic-vs-natural comparison | `final_demo_pack/figures/05_synthetic_vs_natural_correction_success.png`, `16_natural_vs_synthetic_comparison.png`; `final_comparison/figures/11_synthetic_vs_natural_transfer.png` |
| Pipeline architecture diagram | not found as an existing figure — new, to be authored here from the Step 0/1 pipeline map |

## What's genuinely new here

Only the **pipeline architecture diagram** has no existing equivalent — every other
planned category already has at least one reusable, reproducible source figure. The
architecture diagram should be built directly from `../README.md` §11's conceptual
pipeline and the Step 0 audit's per-stage table, not redrawn from memory.

## Not done in this step

No figures are generated or copied here yet. This README only records the plan and the
reuse mapping above, per this step's scope (structure and provenance only).
