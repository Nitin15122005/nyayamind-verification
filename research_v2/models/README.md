# Model cache policy

Weights are loaded from the Hugging Face cache at runtime and must not be copied into this repository. Record resolved revisions and package versions with each run. Required identifiers are fixed in `config/experiment.json`.
