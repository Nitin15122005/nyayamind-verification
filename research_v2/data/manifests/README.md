# Read-only dataset references

Fixtures remain at their audited existing paths; this directory records references and hashes. No benchmark data or gold labels are copied or changed. Run IDs and generated predictions belong under `results/runs/`.
